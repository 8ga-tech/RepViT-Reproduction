# report/ppt_svg —— 答辩 PPT 的合并前设计源（设计记录，不是交付物）

本目录下的 15 份 SVG 是 `report/答辩PPT_RepViT.pptx` 的**合并前设计源**（早期逐页设计稿），只作设计记录与历史参照；**权威交付物是 `report/答辩PPT_RepViT.pptx`**，两者不一致时**一律以 PPTX 为准**。

为容纳试题要求的可视化（≥8 张测试集预测 / ≥4 张同图对比 / ≥5 张训练集外实拍），最终版已把 P03（M0.9 结构图）与 P04（为什么不是标准 ViT）**合并为一页**：因此 `03_arch.svg` 与 `04_notvit.svg` 对应的是合并前的两个独立页面，并且**全部文件的页脚编号仍是合并前的旧页序** —— `01_cover.svg`/`02_status.svg` → 最终 P01/P02，`05..08` 与 `10..11` 号文件 → 最终页序 −1（例如 `05_pretrained.svg` 页脚是 05，对应最终版 P04），`12..15` 号文件 → 与最终页序相同；**不要为了对齐而批量改这些页脚编号**，本注记就是用来替代那次改动的。

`09_results.svg`（8 组实验定量结果表）对应的旧 P09 已被最终版的**训练曲线页（P08）**取代，该文件仅为历史设计记录，**不作为答辩口径**；最终版另有新增的 P11「预测结果」页，本目录中没有它的设计源。

另外：本目录**没有任何脚本会自动消费这些 SVG 重建 PPTX**；PPTX 与 `report/PPT_CONTENT.md` 由 `python tools/update_defense.py` 统一维护，PDF 由 `python tools/export_defense_pdf.py` 用 PowerPoint COM 导出。

## 内容修正记录（2026-09-16，任务 t15）

官方模型评价口径由「自建 1000 张分层子集」整体变更为 **ImageNetV2 matched-frequency 1000 张确定性固定子集** 后，本目录 5 份设计源的**内容**已同步修正：`01_cover.svg`（封面卡 78.20% → 68.70%、口径标签改「ImageNetV2 固定子集 Top-1」、ONNX 3 型号 → 入库 5 型号）、`02_status.svg`（阶段 01 卡 78.20% → 68.70%；阶段 06 卡 P50 7.1~32.7 → 7.6~34.4 ms）、`05_pretrained.svg`（标题/副标题改 ImageNetV2 口径；表体改 6 列并移除「官方公布」列；5 个型号的参数量/MACs/文件大小/Top-1/Top-5 全部换为现行实测；口径边界三条改写）、`14_perf.svg`（性能表 6 个模型的 mean/P50/P95 与 5 个 Top-1 换为 `outputs/benchmarks/summary.csv` 与 `outputs/pretrained_eval/*/metrics.json` 现行值；边际收益面板按新值重算并标注口径）、`15_summary.svg`（结论 ①~④ 与问题 02 按最终版 P15 改写）。

**本次只改内容（数字与口径措辞），一份文件的页脚编号都未改动** —— 各文件页脚仍为合并前旧页序（02 / 05 / 14 / 15 等），逐行与修改前一致；其余 10 份 SVG 未改动。权威口径仍以 `report/答辩PPT_RepViT.pptx` 为准。

### 后续内容对齐（任务 t18，2026-09-16）

`outputs/advanced/marginal_returns.csv` 由 t17 重跑后，其 `lat_p50_ms` = 7.684 / 10.404 / 10.929 / 18.437 / 34.410，`marginal_acc_per_ms` = 0.18382 / 3.04762 / 0.10655 / 0.12521。`14_perf.svg` 的**边际收益面板**按该 CSV 的**全精度值**重算并取 2 位小数：**+0.18 / +3.05 / +0.11 / +0.13 acc / ms**，与 **t18 轮次当时的** CSV 逐项一致（该轮 CSV 的对应值为 `0.10655` / `0.12521`；这两处与 `+0.11` / `+0.13` 已随 t19 重跑被现行值取代，见下节；t15 时用整数百分点差值估算得 +3.03 / +0.08 / +0.14，已废弃）。

> 上段为 t18 当时的记录：其中 `0.10655`、`0.12521` 与 `+0.11`、`+0.13` 四处数值**已被现行 CSV 取代**，见下节。

**本段同样只改内容，页脚编号未动**；本次仅触及 `14_perf.svg` 一个文件。

### 数值再对齐（任务 t22，2026-09-17）

`outputs/advanced/marginal_returns.csv` 在 t19 修正 `family_summary` 后随帕累托表一并重跑，`marginal_acc_per_ms` 现行值为 **0.18382352941176472 / 3.0476190476190346 / 0.0799147575919031 / 0.13773242346459583**（对应 `lat_p50_ms` = 7.684 / 10.404 / 10.929 / 18.437 / 34.410，与 t18 段一致）。**变动只来自 `d_top1`**：t19 把 `repvit_m1_5_in1k` 的 Top-1 由 71.6 更正为 71.4（与 `outputs/pretrained_eval/repvit_m1_5/metrics.json` 对齐）后，M1.1→M1.5 的 `d_top1` 由 0.8 变 0.6（0.6 / 7.508 = 0.0799…），M1.5→M2.3 由 2.0 变 2.2（2.2 / 15.973 = 0.1377…）；`d_lat` 两条均未变。`14_perf.svg` 的**边际收益面板**已按现行 CSV 的**全精度值**现场重算并取 2 位小数：

| 面板分组 | 模型对 | 现行 CSV `marginal_acc_per_ms` | SVG 文本（2 位小数） |
| --- | --- | --- | --- |
| `m-1` | M0.9 → M1.0 | 0.18382352941176472 | +0.18 acc/ms |
| `m-2` | M1.0 → M1.1 | 3.0476190476190346 | +3.05 acc/ms |
| `m-3` | M1.1 → M1.5 | 0.0799147575919031 | +0.08 acc/ms |
| `m-4` | M1.5 → M2.3 | 0.13773242346459583 | +0.14 acc/ms |

四个数值均由 `outputs/advanced/marginal_returns.csv` 现场推导（脚本在仓库外 `_tmp/`），**与 CSV 逐项一致**：`m-1`/`m-2` 两格未变，本次仅把 `m-3` 的 `+0.11` 改为 `+0.08`、`m-4` 的 `+0.13` 改为 `+0.14`（共 2 行），其余字节与页脚编号一字未动。

**本段同样只改内容，页脚编号未动**；本次在 `report/ppt_svg/` 内仅触及 `14_perf.svg` 一个文件（外加本 README 自身）。
